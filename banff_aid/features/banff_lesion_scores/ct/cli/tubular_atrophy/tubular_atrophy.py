def calculate_ct(tubules: dict[str, dict]) -> int:
    """Calculate ct Banff lesion score.

    Args:
    tubules (dict[str, dict]): Dictionary containing JSON-formatted annotations
                              for tubules.

    Return value (int): ct score.
    """