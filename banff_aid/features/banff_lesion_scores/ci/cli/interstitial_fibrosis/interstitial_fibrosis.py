def calculate_ci(tubules: dict[str, dict]) -> int:
    """Calculate ci Banff lesion score.

    Args:
    tubules (dict[str, dict]): Dictionary containing JSON-formatted annotations
                              for tubules.

    Return value (int): ci score.
    """